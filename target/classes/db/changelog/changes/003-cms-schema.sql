-- liquibase formatted sql

-- changeset tricure:3
CREATE TABLE service (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    title VARCHAR(255),
    description TEXT,
    icon VARCHAR(100),
    content TEXT,
    created_at TIMESTAMP
);

CREATE TABLE team_member (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255),
    role VARCHAR(255),
    bio TEXT,
    image_url VARCHAR(500),
    linkedin_url VARCHAR(500),
    created_at TIMESTAMP
);

CREATE TABLE testimonial (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    client_name VARCHAR(255),
    client_role VARCHAR(255),
    quote TEXT,
    image_url VARCHAR(500),
    created_at TIMESTAMP
);

CREATE TABLE faq (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    question TEXT,
    answer TEXT,
    created_at TIMESTAMP
);

CREATE TABLE inquiry (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    name VARCHAR(255),
    email VARCHAR(255),
    subject VARCHAR(255),
    message TEXT,
    is_read BOOLEAN DEFAULT FALSE,
    created_at TIMESTAMP
);

-- Insert some default data
INSERT INTO service (title, description, icon, content, created_at) VALUES 
('Custom Software Development', 'Tailored software solutions to meet your specific business needs.', 'CodeBracketIcon', 'Full cycle development...', CURRENT_TIMESTAMP),
('Lab Integration', 'Seamless integration of laboratory machines with LIS.', 'BeakerIcon', 'Connect your analyzers...', CURRENT_TIMESTAMP),
('Cloud Solutions', 'Scalable cloud infrastructure and migration services.', 'CloudIcon', 'Move to the cloud...', CURRENT_TIMESTAMP);

INSERT INTO team_member (name, role, bio, image_url, created_at) VALUES
('John Doe', 'CEO & Founder', 'Visionary leader with 15 years of experience.', 'https://images.unsplash.com/photo-1560250097-0b93528c311a?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80', CURRENT_TIMESTAMP),
('Jane Smith', 'CTO', 'Tech expert specializing in medical software.', 'https://images.unsplash.com/photo-1573496359142-b8d87734a5a2?ixlib=rb-4.0.3&auto=format&fit=crop&w=800&q=80', CURRENT_TIMESTAMP);

INSERT INTO faq (question, answer, created_at) VALUES
('What industries do you serve?', 'We primarily serve the healthcare and medical laboratory industries.', CURRENT_TIMESTAMP),
('Do you offer support?', 'Yes, we offer 24/7 technical support for all our products.', CURRENT_TIMESTAMP);
