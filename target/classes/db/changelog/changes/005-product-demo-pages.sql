-- liquibase formatted sql

-- changeset admin:005-product-demo-pages
CREATE TABLE product_demo_page (
    id BIGINT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    image_url VARCHAR(255),
    caption VARCHAR(255),
    sort_order INT,
    product_id BIGINT,
    CONSTRAINT fk_product_demo_page_product FOREIGN KEY (product_id) REFERENCES product(id)
);
